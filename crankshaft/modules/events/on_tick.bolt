from ../event_handler import Event


def handler(event):
    merge function_tag minecraft:tick {"replace": false, "values": [event.path('main')]}

    function event.path('main'):
        event.trigger()

on_tick = Event(handler=handler, event_id='on_tick')
